package jetbrains.mps.internalCollections.test.compareOperations;

/*Generated by MPS */

import org.junit.jupiter.api.Test;
import java.util.List;
import java.util.ArrayList;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import org.junit.Assert;
import java.util.Set;
import java.util.HashSet;
import jetbrains.mps.internal.collections.runtime.SetSequence;

public class EqualsListSequence_Test {
  @Test
  public void test_listEqualsListSequence() throws Exception {
    // Why all these tests you may ask? Because this depending on the direction taken, this used not to work (a :eq: b == true but b :eq: a == false)
    List<Integer> list = new ArrayList<>();
    list.add(1);
    list.add(2);
    List<Integer> seq = ListSequence.fromArray(1, 2);
    Assert.assertTrue(list.equals(seq));
  }
  @Test
  public void test_listSequenceEqualsList() throws Exception {
    List<Integer> list = new ArrayList<>();
    list.add(1);
    list.add(2);
    List<Integer> seq = ListSequence.fromArray(1, 2);
    Assert.assertTrue(seq.equals(list));
  }
  @Test
  public void test_setEqualsSetSequence() throws Exception {
    Set<Integer> set = new HashSet<>();
    set.add(1);
    set.add(2);
    Set<Integer> seq = SetSequence.fromArray(1, 2);
    Assert.assertTrue(set.equals(seq));
  }
  @Test
  public void test_setSequenceEqualsSet() throws Exception {
    Set<Integer> set = new HashSet<>();
    set.add(1);
    set.add(2);
    Set<Integer> seq = SetSequence.fromArray(1, 2);
    Assert.assertTrue(seq.equals(set));
  }
  @Test
  public void test_setSequenceEqualsListSequence() throws Exception {
    List<Integer> list = ListSequence.fromArray(1, 2);
    Set<Integer> set = SetSequence.fromArray(1, 2);
    Assert.assertFalse(list.equals(set));
    Assert.assertFalse(set.equals(list));
  }
}
