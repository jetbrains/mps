package jetbrains.mps.baseLanguage.collections.unittest.simple_operations;

/*Generated by MPS */

import org.junit.jupiter.api.Test;
import jetbrains.mps.internal.collections.runtime.Sequence;
import jetbrains.mps.baseLanguage.closures.runtime.YieldingIterator;
import java.util.List;
import org.junit.Assert;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import org.jetbrains.mps.openapi.model.SNode;

public class Concat_Test {
  @Test
  public void test_test1() throws Exception {
    Iterable<Integer> s1 = Sequence.fromClosure(() -> {
      return (Iterable<Integer>) () -> {
        return new YieldingIterator<Integer>() {
          private int __CP__ = 0;
          protected boolean moveToNext() {
__loop__:
            do {
__switch__:
              switch (this.__CP__) {
                case -1:
                  assert false : "Internal error";
                  return false;
                case 2:
                  this.__CP__ = 3;
                  this.yield(1);
                  return true;
                case 3:
                  this.__CP__ = 4;
                  this.yield(2);
                  return true;
                case 4:
                  this.__CP__ = 1;
                  this.yield(3);
                  return true;
                case 0:
                  this.__CP__ = 2;
                  break;
                default:
                  break __loop__;
              }
            } while (true);
            return false;
          }
        };
      };
    });
    Iterable<Integer> s2 = Sequence.fromClosure(() -> {
      return (Iterable<Integer>) () -> {
        return new YieldingIterator<Integer>() {
          private int __CP__ = 0;
          protected boolean moveToNext() {
__loop__:
            do {
__switch__:
              switch (this.__CP__) {
                case -1:
                  assert false : "Internal error";
                  return false;
                case 2:
                  this.__CP__ = 3;
                  this.yield(3);
                  return true;
                case 3:
                  this.__CP__ = 4;
                  this.yield(4);
                  return true;
                case 4:
                  this.__CP__ = 1;
                  this.yield(5);
                  return true;
                case 0:
                  this.__CP__ = 2;
                  break;
                default:
                  break __loop__;
              }
            } while (true);
            return false;
          }
        };
      };
    });
    List<Integer> s3 = Sequence.fromIterable(s1).concat(Sequence.fromIterable(s2)).toList();
    Assert.assertTrue((Integer) ListSequence.fromList(s3).first() == 1);
    Assert.assertTrue((Integer) ListSequence.fromList(s3).getElement(1) == 2);
    Assert.assertTrue((Integer) ListSequence.fromList(s3).getElement(2) == 3);
    Assert.assertTrue((Integer) ListSequence.fromList(s3).getElement(3) == 3);
    Assert.assertTrue((Integer) ListSequence.fromList(s3).getElement(4) == 4);
    Assert.assertTrue((Integer) ListSequence.fromList(s3).getElement(5) == 5);
    // null test
    s1 = null;
    Iterable<Integer> s2_ = Sequence.fromIterable(s1).concat(Sequence.fromIterable(s2));
    Assert.assertEquals(Integer.valueOf(3), Integer.valueOf(Sequence.fromIterable(s2_).count()));
    Iterable<Integer> s2__ = Sequence.fromIterable(s2).concat(null);
    Assert.assertEquals(Integer.valueOf(3), Integer.valueOf(Sequence.fromIterable(s2__).count()));
  }
  @Test
  public void test_mps36315() throws Exception {
    Iterable<SNode> classifiers = null;
    Iterable<SNode> methods = null;
    Iterable<SNode> vars = null;

    // Rather some typesystem test
    Iterable<SNode> seq = Sequence.fromIterable(classifiers).concat(Sequence.fromIterable(methods)).concat(Sequence.fromIterable(vars));
    Assert.assertTrue(Sequence.fromIterable(seq).isEmpty());
  }
}
