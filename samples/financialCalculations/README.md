Financial Calculator
====================

This sample project illustrates the possibilities to use MPS-based DSLs for specification of financial calculations. The language constructs allow the user to define interactive forms with input fields, selectors, check boxes, internal or external functions as well as output field to show the results. Output of tabular data is also supported.

