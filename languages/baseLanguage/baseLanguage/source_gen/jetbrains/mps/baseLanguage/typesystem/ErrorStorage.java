package jetbrains.mps.baseLanguage.typesystem;

/*Generated by MPS */

import java.util.Map;
import jetbrains.mps.util.Pair;
import org.jetbrains.mps.openapi.model.SNode;
import jetbrains.mps.internal.collections.runtime.MapSequence;
import java.util.HashMap;
import java.util.List;
import jetbrains.mps.internal.collections.runtime.ISelector;
import jetbrains.mps.internal.collections.runtime.IMapping;

/*package*/ class ErrorStorage {
  private final Map<Pair<SNode, ErrorTypes>, String> myNodePlusType2Msg = MapSequence.fromMap(new HashMap<Pair<SNode, ErrorTypes>, String>());

  /*package*/ void putError(SNode node, ErrorTypes type, String msg) {
    Pair newPair = new Pair(node, type);
    if (!(MapSequence.fromMap(myNodePlusType2Msg).containsKey(newPair))) {
      MapSequence.fromMap(myNodePlusType2Msg).put(newPair, msg);
    }
  }

  /*package*/ List<ErrorStorage.Error> getErrorList() {
    return MapSequence.fromMap(myNodePlusType2Msg).toListSequence().select(new ISelector<IMapping<Pair<SNode, ErrorTypes>, String>, ErrorStorage.Error>() {
      public ErrorStorage.Error select(IMapping<Pair<SNode, ErrorTypes>, String> it) {
        return new ErrorStorage.Error(it.key().o1, it.key().o2, it.value());
      }
    }).toListSequence();
  }

  /*package*/ static class Error {
    /*package*/ final SNode node;
    /*package*/ final ErrorTypes type;
    /*package*/ final String msg;

    /*package*/ Error(SNode node, ErrorTypes type, String msg) {
      this.node = node;
      this.type = type;
      this.msg = msg;
    }
  }
}
