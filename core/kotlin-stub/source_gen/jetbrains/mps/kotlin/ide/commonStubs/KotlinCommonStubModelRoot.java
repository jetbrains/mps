package jetbrains.mps.kotlin.ide.commonStubs;

/*Generated by MPS */

import jetbrains.mps.annotations.GeneratedClass;
import jetbrains.mps.extapi.persistence.FileBasedModelRoot;
import jetbrains.mps.java.stub.PackageScopeControl;
import org.jetbrains.annotations.NotNull;
import org.jetbrains.mps.openapi.model.SModel;
import java.util.List;
import jetbrains.mps.vfs.IFile;
import jetbrains.mps.extapi.persistence.SourceRootKinds;
import java.util.Set;
import jetbrains.mps.internal.collections.runtime.SetSequence;
import java.util.HashSet;
import jetbrains.mps.internal.collections.runtime.ISelector;
import jetbrains.mps.vfs.path.Path;
import jetbrains.mps.internal.collections.runtime.IVisitor;
import java.util.Map;
import org.jetbrains.mps.openapi.model.SModelId;
import jetbrains.mps.internal.collections.runtime.MapSequence;
import java.util.HashMap;
import org.jetbrains.mps.openapi.model.SModelReference;
import jetbrains.mps.kotlin.stubs.common.KotlinLanguage;
import jetbrains.mps.extapi.model.SModelBase;
import jetbrains.mps.extapi.persistence.FolderSetDataSource;
import org.jetbrains.mps.openapi.persistence.Memento;
import org.jetbrains.annotations.Nullable;

@GeneratedClass(node = "r:6c6710f1-72ef-4241-9ac5-bafd05beea2c(jetbrains.mps.kotlin.ide.commonStubs)/2728382324535685903", model = "r:6c6710f1-72ef-4241-9ac5-bafd05beea2c(jetbrains.mps.kotlin.ide.commonStubs)")
public class KotlinCommonStubModelRoot extends FileBasedModelRoot implements JarHelper.ModelDescriptorProducer {
  /**
   * TODO use it?
   */
  private PackageScopeControl packageScope;

  @Override
  public String getType() {
    return KotlinCommonConstants.KOTLIN_COMMON_ROOT;
  }

  @NotNull
  @Override
  public Iterable<SModel> loadModels() {
    // TODO merge with java by setting this public
    List<IFile> files = JarHelper.rootsToFiles(getSourceRoots(SourceRootKinds.SOURCES));
    List<IFile> excluded = JarHelper.rootsToFiles(getSourceRoots(SourceRootKinds.EXCLUDED));

    Set<IFile> jarsToLoad = SetSequence.fromSet(new HashSet<IFile>());

    final Set<IFile> cpRootsToLoad = SetSequence.fromSet(new HashSet<IFile>());
    for (IFile file : files) {
      JarHelper.collectJarFiles(file, excluded, jarsToLoad);

      // we suppose here that each path can be either a jar-file or a classes directory or a jar directory,
      // but does not contain both jar files and class files
      if (SetSequence.fromSet(jarsToLoad).isNotEmpty()) {
        continue;
      }

      SetSequence.fromSet(cpRootsToLoad).addElement(file);
    }

    // FIXME though IFile("whatever.jar") could be already from JAR FS (e.g. CommonPaths creates IFiles using JAR FS for jar files right away), I found no way to figure it out
    // therefore have to resort to this stupid way to step into jar
    SetSequence.fromSet(jarsToLoad).select(new ISelector<IFile, IFile>() {
      public IFile select(IFile it) {
        return getFileSystem().getFile(it.getPath() + Path.ARCHIVE_SEPARATOR);
      }
    }).visitAll(new IVisitor<IFile>() {
      public void visit(IFile it) {
        SetSequence.fromSet(cpRootsToLoad).addElement(it);
      }
    });

    final Map<SModelId, SModel> result = MapSequence.fromMap(new HashMap<SModelId, SModel>());
    SetSequence.fromSet(cpRootsToLoad).visitAll(new IVisitor<IFile>() {
      public void visit(IFile it) {
        getModelDescriptors(result, it, "", getModule(), packageScope, KotlinCommonStubModelRoot.this);
      }
    });

    return MapSequence.fromMap(result).values();
  }

  @Override
  public boolean isFileIncluded(IFile file) {
    return KotlinCommonConstants.isKotlinLib(file.getName());
  }
  @Override
  public SModelReference createPackageModelReference(String packageName) {
    return KotlinLanguage.createSModelReference(packageName, getModule().getModuleReference());
  }
  @Override
  public SModelBase createDescriptor(SModelReference reference, FolderSetDataSource dataSource) {
    return new KotlinCommonStubModelDescriptor(reference, dataSource);
  }

  @Override
  public void load(@NotNull Memento memento) {
    super.load(memento);
  }
  @Deprecated
  @Override
  public boolean canCreateModel(@NotNull String string) {
    return false;
  }
  @Nullable
  @Override
  public SModel getModel(@NotNull SModelId id) {
    // TODO not implemented in BL either
    return null;
  }
  @Nullable
  @Override
  public SModel createModel(@NotNull String string) {
    return null;
  }

}
