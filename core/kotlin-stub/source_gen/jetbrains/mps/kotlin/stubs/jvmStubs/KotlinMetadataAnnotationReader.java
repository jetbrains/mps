package jetbrains.mps.kotlin.stubs.jvmStubs;

/*Generated by MPS */

import jetbrains.mps.annotations.GeneratedClass;
import org.jetbrains.org.objectweb.asm.AnnotationVisitor;
import org.jetbrains.org.objectweb.asm.Opcodes;
import java.util.Objects;
import java.util.List;
import java.util.ArrayList;
import kotlinx.metadata.jvm.KotlinClassHeader;

/**
 * Object responsible for getting data from kotlin.Metadata annotation
 */
@GeneratedClass(node = "r:bdaa2532-d0d0-46ce-8145-d47be9b807a4(jetbrains.mps.kotlin.stubs.jvmStubs)/8317865157553686594", model = "r:bdaa2532-d0d0-46ce-8145-d47be9b807a4(jetbrains.mps.kotlin.stubs.jvmStubs)")
public class KotlinMetadataAnnotationReader extends AnnotationVisitor {
  private int k;
  private int xi;
  private int[] mv = new int[0];
  private String[] d1 = new String[0];
  private String[] d2 = new String[0];

  /*package*/ KotlinMetadataAnnotationReader() {
    super(Opcodes.API_VERSION);
  }

  @Override
  public void visit(String name, Object value) {
    if (Objects.equals(name, "k") && value instanceof Integer) {
      k = (int) value;
    } else if (Objects.equals(name, "mv") && value instanceof int[]) {
      mv = (int[]) value;
    } else if (Objects.equals(name, "x1") && value instanceof Integer) {
      xi = (int) value;
    }
  }

  @Override
  public AnnotationVisitor visitArray(final String name) {
    // This only seems to visit string arrays
    final List<String> strings = new ArrayList<>();

    return new AnnotationVisitor(Opcodes.API_VERSION) {
      @Override
      public void visit(String name, Object value) {
        if (value instanceof String) {
          strings.add((String) value);
        }
      }
      @Override
      public void visitEnd() {
        if ("d1".equals(name)) {
          d1 = strings.toArray(new String[0]);
        } else
        if ("d2".equals(name)) {
          d2 = strings.toArray(new String[0]);
        }
      }
    };
  }


  public KotlinClassHeader export(String packageName) {
    return new KotlinClassHeader(k, mv, d1, d2, null, packageName, xi);
  }
}
