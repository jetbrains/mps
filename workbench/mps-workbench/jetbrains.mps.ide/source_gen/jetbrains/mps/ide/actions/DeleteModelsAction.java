package jetbrains.mps.ide.actions;

/*Generated by MPS */

import java.util.List;
import org.jetbrains.mps.openapi.model.SModel;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import jetbrains.mps.smodel.SModelStereotype;
import org.jetbrains.mps.openapi.module.SModule;
import jetbrains.mps.project.Project;
import org.jetbrains.mps.openapi.module.SRepository;
import jetbrains.mps.ide.save.SaveRepositoryCommand;
import jetbrains.mps.workbench.actions.model.DeleteModelHelper;

public class DeleteModelsAction {
  public static boolean isApplicable(List<SModel> models) {
    for (SModel m : ListSequence.fromList(models)) {
      if (!(SModelStereotype.isStubModel(m)) && !(SModelStereotype.isDescriptorModel(m))) {
        return true;
      }
    }
    return false;
  }
  public static void executeAction(final List<SModel> models, final boolean safeDelete, final SModule contextModule, final Project project) {
    final SRepository repository = project.getRepository();
    repository.getModelAccess().executeCommandInEDT(new Runnable() {
      public void run() {
        // see MPS-18743 
        new SaveRepositoryCommand(repository).execute();
        for (SModel model : ListSequence.fromList(models)) {
          if (SModelStereotype.isStubModel(model) || SModelStereotype.isDescriptorModel(model)) {
            continue;
          }
          DeleteModelHelper.deleteModel(project, contextModule, model, safeDelete, true);
        }
      }
    });
  }
}
