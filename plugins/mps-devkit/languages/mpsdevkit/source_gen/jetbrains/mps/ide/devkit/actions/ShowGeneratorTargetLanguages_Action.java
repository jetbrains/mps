package jetbrains.mps.ide.devkit.actions;

/*Generated by MPS */

import jetbrains.mps.workbench.action.BaseAction;
import javax.swing.Icon;
import jetbrains.mps.workbench.action.ActionAccess;
import com.intellij.openapi.actionSystem.AnActionEvent;
import java.util.Map;
import jetbrains.mps.project.MPSProject;
import jetbrains.mps.ide.actions.MPSCommonDataKeys;
import org.jetbrains.mps.openapi.module.SModule;
import jetbrains.mps.smodel.Generator;
import org.jetbrains.annotations.NotNull;
import jetbrains.mps.ide.messages.DefaultMessageHandler;
import jetbrains.mps.generator.impl.plan.TemplateModelScanner;
import jetbrains.mps.smodel.SModelStereotype;
import jetbrains.mps.messages.Message;
import org.jetbrains.mps.openapi.language.SLanguage;
import jetbrains.mps.smodel.language.LanguageRegistry;
import jetbrains.mps.smodel.language.GeneratorRuntime;
import jetbrains.mps.generator.runtime.TemplateModule;
import java.util.Collection;

public class ShowGeneratorTargetLanguages_Action extends BaseAction {
  private static final Icon ICON = null;

  public ShowGeneratorTargetLanguages_Action() {
    super("Show Target Languages", "", ICON);
    this.setIsAlwaysVisible(false);
    this.setActionAccess(ActionAccess.READ_PROJECT);
    updateInBackground(true);
  }
  @Override
  public boolean isDumbAware() {
    return true;
  }
  @Override
  protected boolean collectActionData(AnActionEvent event, final Map<String, Object> _params) {
    if (!(super.collectActionData(event, _params))) {
      return false;
    }
    {
      MPSProject p = event.getData(MPSCommonDataKeys.MPS_PROJECT);
      if (p == null) {
        return false;
      }
    }
    {
      SModule p = event.getData(MPSCommonDataKeys.MODULE);
      if (p == null) {
        return false;
      }
      if (p != null && !(p instanceof Generator)) {
        return false;
      }
    }
    return true;
  }
  @Override
  public void doExecute(@NotNull final AnActionEvent event, final Map<String, Object> _params) {
    final DefaultMessageHandler mh = new DefaultMessageHandler(event.getData(MPSCommonDataKeys.MPS_PROJECT).getProject());
    final TemplateModelScanner ms = new TemplateModelScanner();
    ((Generator) event.getData(MPSCommonDataKeys.MODULE)).getModels(SModelStereotype::isGeneratorModel).forEach(ms::scan);
    mh.handle(Message.info(getClass(), String.format("Languages in use by generator %s", ((Generator) event.getData(MPSCommonDataKeys.MODULE)).getModuleName()), null, null));
    mh.handle(Message.info(getClass(), String.format("  Target languages (%d):", ms.getTargetLanguages().size()), null, null));
    for (SLanguage l : ms.getTargetLanguages()) {
      mh.handle(Message.info(getClass(), String.format("    %s", l.getQualifiedName()), null, null));
    }
    mh.handle(Message.info(getClass(), String.format("  Query languages (%d):", ms.getQueryLanguages().size()), null, null));
    for (SLanguage l : ms.getQueryLanguages()) {
      mh.handle(Message.info(getClass(), String.format("    %s", l.getQualifiedName()), null, null));
    }
    LanguageRegistry lr = event.getData(MPSCommonDataKeys.MPS_PROJECT).getComponent(LanguageRegistry.class);
    GeneratorRuntime grt = lr.getGenerator(((Generator) event.getData(MPSCommonDataKeys.MODULE)).getModuleReference());
    if (grt instanceof TemplateModule) {
      mh.handle(Message.info(getClass(), "", null, null));
      Collection<SLanguage> targetLanguages = ((TemplateModule) grt).getTargetLanguages();
      mh.handle(Message.info(getClass(), String.format("  Deployed information, target languages (%d):", targetLanguages.size()), null, null));
      for (SLanguage l : targetLanguages) {
        mh.handle(Message.info(getClass(), String.format("    %s", l.getQualifiedName()), null, null));
      }
    }
  }
}
