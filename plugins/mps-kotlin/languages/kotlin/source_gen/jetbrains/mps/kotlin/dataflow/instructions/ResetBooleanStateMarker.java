package jetbrains.mps.kotlin.dataflow.instructions;

/*Generated by MPS */

import org.jetbrains.annotations.ApiStatus;

/**
 * This singleton can be used in a write instruction in kotlin dataflow to ask for the boolean state to be reset.
 * 
 * This should be used:
 * - before a ifjump where you would like all current state to exist on both branches.
 * - before a condition with a similar ifjump after: all condition may not create adequate branches
 * 
 * Node associated with the write instruction has no meaning, this is a hack to emulate a new instruction before we
 * come up with a generic way usable with more languages.
 * 
 * Experimental / hacky: should be replaced with appropriate mechanism in the future (proper implementation of a value stack for
 * all expressions with some push/pop instruction would remove the need for such reset)
 */
@ApiStatus.Experimental
public class ResetBooleanStateMarker {
  private static ResetBooleanStateMarker INSTANCE;
  private ResetBooleanStateMarker() {
  }
  public static ResetBooleanStateMarker get() {
    if (INSTANCE == null) {
      INSTANCE = new ResetBooleanStateMarker();
    }
    return INSTANCE;
  }
}
