package jetbrains.mps.kotlin.runtime.types.identifiers;

/*Generated by MPS */

import java.util.Objects;

public abstract class IdBasedType<T> implements TypeKey {
  protected final T myKey;
  public IdBasedType(T key) {
    myKey = key;
  }
  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || this.getClass() != o.getClass()) {
      return false;
    }
    return Objects.equals(myKey, ((IdBasedType) o).myKey);
  }
  @Override
  public int hashCode() {
    return myKey.hashCode();
  }

  @Override
  public String toString() {
    return this.getClass().getName() + "{" + "key=" + myKey + "}";
  }
}
