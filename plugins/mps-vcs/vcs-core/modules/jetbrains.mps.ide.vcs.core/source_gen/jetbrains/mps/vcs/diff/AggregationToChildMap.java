package jetbrains.mps.vcs.diff;

/*Generated by MPS */

import jetbrains.mps.annotations.GeneratedClass;
import java.util.Map;
import org.jetbrains.mps.openapi.language.SContainmentLink;
import java.util.List;
import org.jetbrains.mps.openapi.model.SNode;
import java.util.HashMap;
import org.jetbrains.mps.openapi.model.SNodeId;
import org.jetbrains.annotations.NotNull;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import jetbrains.mps.lang.smodel.generator.smodelAdapter.SNodeOperations;
import java.util.ArrayList;
import java.util.function.Function;
import jetbrains.mps.internal.collections.runtime.IListSequence;

/**
 * Groups children of a node per aggregation link (with respect to commented-out nodes) and provides few convenience methods
 */
@GeneratedClass(nodeId = "8126009215930053467", model = "r:5744ed46-c83f-47cd-94ce-f24d1f92d6a1(jetbrains.mps.vcs.diff)")
/*package*/ final class AggregationToChildMap {
  private final Map<SContainmentLink, List<SNode>> myRole2Child = new HashMap<>();
  private final SNodeId myParentId;

  /*package*/ AggregationToChildMap(@NotNull SNode node, boolean respectCommentedOutNodes) {
    myParentId = node.getNodeId();
    // respectCommentedOutNodes is to synchronise knowledge about whether to stick to single role or look 
    // into combination with respective ChildAttributes under smodelAttributes role throughout few classes,
    // where this assumption is now scattered around and is not apparent.
    if (!(respectCommentedOutNodes)) {
      // see MPS-35421, SNodeOperations.getContainingLinkInChildrenAndChildAttributesCollection() case
      ListSequence.fromList(SNodeOperations.getChildren(node)).visitAll((child) -> myRole2Child.computeIfAbsent(child.getContainmentLink(), (SContainmentLink link) -> ListSequence.fromList(new ArrayList<SNode>())).add(child));
    } else {
      ListSequence.fromList(SNodeOperations.getChildren(node)).visitAll((child) -> {
        SContainmentLink link = SNodeOperations.getContainingLinkInChildrenAndChildAttributesCollection(child);
        myRole2Child.computeIfAbsent(link, new Function<SContainmentLink, List<SNode>>() {
          public IListSequence<SNode> apply(SContainmentLink link) {
            return ListSequence.fromList(new ArrayList<SNode>());
          }
        }).add(child);
      });
    }
  }

  /*package*/ Iterable<SContainmentLink> roles() {
    return myRole2Child.keySet();
  }

  @NotNull
  /*package*/ SNodeId getParentId() {
    return myParentId;
  }

  /*package*/ List<SNode> get(SContainmentLink link) {
    return myRole2Child.getOrDefault(link, ListSequence.fromList(new ArrayList<SNode>()));
  }

  /*package*/ List<SNodeId> getIDs(SContainmentLink link) {
    return ListSequence.fromList(get(link)).select((n) -> n.getNodeId()).toList();
  }

}
