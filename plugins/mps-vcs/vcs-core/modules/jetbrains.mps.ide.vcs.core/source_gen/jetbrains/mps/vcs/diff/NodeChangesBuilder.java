package jetbrains.mps.vcs.diff;

/*Generated by MPS */

import jetbrains.mps.annotations.GeneratedClass;
import java.util.List;
import jetbrains.mps.vcs.diff.changes.StructureChange;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import java.util.ArrayList;
import org.jetbrains.annotations.NotNull;
import org.jetbrains.annotations.Nullable;
import org.jetbrains.mps.openapi.model.SNode;
import jetbrains.mps.vcs.diff.changes.AddRootChange;
import jetbrains.mps.vcs.diff.changes.DeleteRootChange;

/**
 * FIXME get back to this code and figure out what's the difference between BasicNodeCHangesBuilder and StructChangeSetBuilder???!?
 */
@GeneratedClass(nodeId = "2412132629081465761", model = "r:5744ed46-c83f-47cd-94ce-f24d1f92d6a1(jetbrains.mps.vcs.diff)")
public abstract class NodeChangesBuilder {
  private final ChangeSet myChangeSet;
  private final List<StructureChange> myChanges = ListSequence.fromList(new ArrayList<>());

  protected NodeChangesBuilder(@NotNull ChangeSet changeSet) {
    myChangeSet = changeSet;
  }

  public final List<StructureChange> buildChanges() {
    return ListSequence.fromListWithValues(new ArrayList<StructureChange>(), myChanges);
  }

  protected final ChangeSet changeSet() {
    return myChangeSet;
  }

  protected final void recordChange(@NotNull StructureChange change) {
    ListSequence.fromList(myChanges).addElement(change);
  }

  public final void buildForRoot(@Nullable SNode oldRoot, @Nullable SNode newRoot) {
    if (oldRoot == null && newRoot == null) {
      return;
    }
    if (oldRoot == null) {
      recordChange(new AddRootChange(myChangeSet, newRoot.getNodeId()));
    } else if (newRoot == null) {
      recordChange(new DeleteRootChange(myChangeSet, oldRoot.getNodeId()));
    } else {
      buildForNode(oldRoot, newRoot);
    }
  }

  protected abstract void buildForNode(@NotNull SNode oldNode, @NotNull SNode newNode);
}
